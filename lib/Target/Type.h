#pragma once

namespace tg {

/**
 * Enumerated types of targets
 *
 * This enum contains all the different types of targets (architectures)
 * supported by this library. When adding a new target, be sure to add a
 * corresponding type to this enum.
 */
enum class TargetType {
    X86_64
};

}
